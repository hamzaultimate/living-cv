{
  "DetailedErrors": true,
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "//": "Local dev uses SQL Server LocalDB (ships with Visual Studio). No secret — safe to commit. Override in your own environment as needed.",
    "Default": "Server=(localdb)\\MSSQLLocalDB;Database=PortfolioDev;Trusted_Connection=True;TrustServerCertificate=True;MultipleActiveResultSets=true"
  }
}
